  <div class="flex flex-col  rounded-lg bg-white">
    <a href="<%= repertoire_path(repertoire) %>" class="group relative block h-48 overflow-hidden bg-gray-100 md:h-64">
      <%= image_tag repertoire.repertoire_image_url, loading: "lazy", class:"absolute object-contain inset-0 h-full w-full object-cover object-center transition duration-200 group-hover:scale-110" %>
    </a>
    <div class="flex flex-1 flex-col p-4 sm:p-6">
      <h2 class="mb-2 text-lg font-semibold text-gray-800">
        <a href="<%= repertoire_path(repertoire) %>" class="transition duration-100 hover:text-indigo-500 active:text-indigo-600"><%= repertoire.name %></a>
      </h2>
      <div class="mt-auto flex items-end justify-between">
        <div class="flex items-center gap-2">
        </div>
        <%= render 'repertoires/crud_menus', repertoire: repertoire %>
      </div>
    </div>
  </div>
